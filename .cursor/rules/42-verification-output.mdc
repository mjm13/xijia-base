---
description: 验证输出门禁（完成声明前必须给证据）
alwaysApply: true
---

# 目标

把“已完成”变成可核验事实：必须报告验证证据、未执行项与风险。

# 约束

1. 不得只说“已完成/已修复”，必须给结构化验证结果。
2. 无法执行的验证项必须写明：未执行 + 原因 + 替代检查。
3. 业务逻辑变更必须有自动化验证（测试或可执行检查）。
4. 优先调用 `verification-before-completion` 技能执行收尾验证。
5. 若触达核心业务代码，verify 阶段必须调用 `xijia-comment-enhancer`（见 `44-comment-sync.mdc`）。
6. verify 阶段需给出 `Code Review` 结果（done 或 skipped+reason）。
7. 需给出 `quality-judge` 判定（`pass|revise`），`revise` 不得宣告完成。

# 输出模板

```text
验证：
- 复现验证：...
- 已执行：...
- 结果：...
- 未执行：...
- 风险/需人工确认：...

编译/静态检查：
- 已执行：...
- 结果：...

自动化测试：
- 新增/更新：...
- 已执行：...
- 结果：...

Comment Sync：
- 触达核心业务代码：yes|no
- 已执行：xijia-comment-enhancer | skipped
- 文件清单 / 跳过原因：...
- 结果：done|blocked

Code Review：
- 已执行：requesting-code-review | skipped
- 结果：done|skipped
- 备注：...

Quality Judge：
- verdict: pass|revise
- reasons: ...
- required_fixes: ...
```
