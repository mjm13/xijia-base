---
description: 后端实现主旨护栏（技术栈无关）
globs:
  - "backend/**"
---

# 主旨

后端实现以“业务规则正确性 + 可验证性 + 可追溯性”为核心，分层可裁剪但职责必须清晰。

# 硬约束（Must / Must Not）

- 涉及数据访问的测试必须基于真实数据库行为，禁止用 mock 伪造数据层真值。
- 外部系统依赖可 mock，但数据库语义与业务不变量不可 mock。
- 测试样本必须满足领域不变量与契约约束。
- 发生契约冲突时，不得在实现层“猜测修复”，必须先升级到规格/契约层统一。

# 升级触发（When to Escalate）

- 需求从弱规则演进为强不变量/跨聚合一致性时，必须升级分层设计并补充设计/ADR 说明。
- 测试无法覆盖关键业务规则时，必须先补验证方案再继续实现。
- 文档与测试冲突时，以可执行测试为准并回写文档。

# 验收口径（Done Means）

- 关键业务规则在真实数据语义下可复现、可验证。
- 分层选择有明确理由，且与当前复杂度匹配。
- 交付内容满足契约并可被回归验证。
