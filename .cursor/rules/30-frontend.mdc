---
description: 前端实现主旨护栏（技术栈无关）
globs:
  - "frontend/**"
---

# 主旨

前端实现以“契约一致性 + 能力边界清晰 + 可维护交互”为核心，避免页面层承载业务规则真相。

# 硬约束（Must / Must Not）

- 前后端交互必须遵循已确认契约（路径、参数、返回语义）。
- 模块命名与能力边界保持一致，禁止跨模块隐式耦合。
- 不在前端固化后端业务真值；业务规则以后端与契约层为准。
- 出现契约冲突时，先升级规格/契约，再调整实现。

# 升级触发（When to Escalate）

- 接口语义不一致、字段含义不明确、权限边界冲突时，必须升级到规格层决策。
- 组件职责失衡（展示层混入业务编排）时，必须拆分并回到模块边界。
- 无法用现有契约覆盖交互场景时，必须先补契约再编码。

# 验收口径（Done Means）

- 交互行为、接口契约、能力命名三者一致。
- 变更可通过回归验证，且不引入跨模块语义漂移。
