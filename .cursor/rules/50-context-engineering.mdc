---
description: 上下文工程门禁（按 BC just-in-time 加载，避免上下文污染）
---

# 目标

把信息放在正确上下文面，避免一次性塞入全量文档导致漂移与误判。

# 规则

1. 关键规则前置：约束与安全规则优先加载。
2. 领域文档按 BC 即时加载：优先 `docs/domain/<bc>/**`，禁止默认整包读取 `docs/domain/**`。
3. 仅在跨 BC 分析时补读 `docs/domain/context-map.md`。
4. 探索型任务优先使用 subagent，隔离上下文污染。
5. 长任务在 checkpoint 写入记忆（决策/结果/来源/置信度）。
6. 定期执行文档卫生检查（`xijia-docs-score`），剪枝低价值文档候选。

# 输出模板

```text
上下文装载说明：
- 当前 BC：...
- 已加载文档：...
- 未加载（原因）：...
- 是否跨 BC：是/否
```
