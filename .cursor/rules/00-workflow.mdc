---
description: xijia 研发宪法（精简版）：分级、路由、门禁与收尾
alwaysApply: true
---

# 核心定位

- 真相源：测试 > spec；代码与已发布文档共同构成事实。
- 流程入口：优先 `/xijia:start`（`xijia-ops-pipeline`）；仅在明确场景使用单步命令。
- 详细执行步骤放在技能中，本文件只保留必须长期生效的门禁。
- 项目级硬约束以 `docs/constitution.md` 为准（由 init 生成）。

# 需求分级与路由

- Gate-0（需求完整性闸门）先于分级执行：必须先判定 `complete|partial|reject`（详见 `45-requirement-intake.mdc`）。
- 分级结论必须附“分级理由”：逐条说明 red 触发器（核心业务 / 复杂规则 / 跨限界上下文）命中或未命中。
- 分级理由建议使用 Tier Decision Matrix（命中项 + 证据 + 置信度），矩阵规则在 `xijia-ops-pipeline` 维护。
- **分级与判型完成后**，必须将 `tier` 与 `type` 写入 requirement 文档 **YAML frontmatter**（机器可读真相源）；正文「分级与判型」章节的 Tier / Change Type 须与 frontmatter 一致；`tier=red` 时另填 `openspec_change`。
- 🟢 轻量：Plan Mode + 人审 + 执行；分级判型/实现方案/验收记录直接写进 requirement 文档（不再单建 `docs/plans/`）。
- 🟢-trivial（绿档快车道）：仅配置/样式/文档/锁文件等低风险改动，且不触达核心业务前缀、不命中 Approval Gates。可跳过 Gate-0 数据流闭环表逐行填写（需显式声明 `本需求无数据流（green-trivial）`），但仍保留 Gate-1/Gate-2 人审。
- 🟡 中等：Plan Mode + 可选 Superpowers；必要时升档。
- 🔴 核心：完整链路 `explore -> propose -> analyze -> apply -> verify -> sync -> archive -> sync-knowledge`。
- 🧪 spike：链路不清时先探针，代码不得直接作为交付成果。

# 人审门禁（硬停，不得自动跳过）

1. Gate-0（需求完整性闸门）：
   - 分级前必须完成完整性判定：`complete|partial|reject`。
   - `partial`/`reject` 必须先向用户报告可落地范围与缺失清单，获得确认后才可继续。
2. Gate-1（实现前批准）：
   - 完成方案/计划后必须停止并请求用户批准。
   - 未获批准前，禁止修改任何非文档代码、迁移、依赖与配置。
3. Gate-2（验收签字与状态迁移）：
   - 勾选验收标准、需求状态迁移（如 inbox -> shipped）、change 归档前，必须先获得用户人工验收确认。
   - Agent 只能提请验收，不得自行标记“已完成”或自动归档。

# Change 类型门禁

- 每个 change 必须先判定 `business|technical|hybrid`。
- `technical`/UI 壳层不得虚构 DDD 领域模型。
- `business`/`hybrid` 的业务术语需要可追溯到领域文档条目。
- `technical` 需求必须使用 `docs/requirements/technical-requirement-template.md` 必填项（SLO/可靠性/安全/回滚/兼容/可观测）。
- 技术实现细节不得写入 `docs/domain/*`；关键技术权衡必须沉淀至 `docs/decisions/*`（ADR）。

# 文档与知识边界

- 开发中领域草稿只写 `docs/openspec/changes/<name>/domain/`。
- 仅在 `sync -> archive -> sync-knowledge` 后提升到 `docs/domain/*`。
- `docs/domain/*` 只存已发布事实，作为 AI 读取现状的唯一领域真相源。
- `docs/architecture.md`、`docs/capability-map.md` 仅允许在收尾阶段更新。
- `AGENTS.md` 为活文档：需求迭代后若命中任一变化（dev/build/test/deploy 命令、顶层目录/模块职责、关键入口、关键外部依赖/技术栈版本、安全边界、提交/PR 约定）必须在收尾阶段增量更新；不得引用已删除脚本/命令。
- DDD 同步提醒：领域文档必须“先判定后更新”，禁止无异议累加。`business/hybrid` 先做 A/B/C/D 分类（新 BC / 改聚合不变量 / 仅字段语义 / 无领域影响）；同词异义或边界冲突先 stop-and-report，人工确认后再写入。

# 收尾门禁（不得跳过）

1. verify：测试/构建有证据。
2. 注释同步：若本次新增或修改核心业务代码（API 端点、Service/业务处理单元、跨模块编排、核心业务/持久化模型），必须在**写代码阶段**调用 `xijia-comment-enhancer` 同步输出或更新语义注释（先内部层/模型层，再端点层）；verify 阶段必须运行 comment-sync guard 兜底校验。纯文档/配置/样式改动可跳过并说明原因（详见 `44-comment-sync.mdc`）。
3. 文档同步：所有档位收尾均执行 `sync-knowledge`，由 `tier` 决定执行范围（red 全量；green/yellow 轻量）。
4. 验收记录：写入对应 requirement 文档的「验收记录」章节（人工可复现）。
5. 收尾聚合自检：进入 Gate-2 前运行 `python .cursor/hooks/pipeline_guard.py --check-release --req <requirement-file>`，客观项（comment-sync / 后端测试存在性 / 审批记录 / ADR 命中校验）exit≠0 即阻断；并按其提示逐条确认人工门禁（code-review / quality-judge / Deferred 入池）。
6. Gate-2：用户人工验收签字后，方可状态迁移/归档；需求文档必须回填 Gate-2 审批人/日期留痕。
7. commit：本需求代码与同步文档一起提交（由用户触发执行）。
8. 对 `partial/reject` 产出的 Deferred 项，必须写入 `docs/requirements/backlog.md` 后方可收尾。

- 未完成第 1-8 步，不得开启下一需求。
- “需求完成”的对外口径为：已达到可提交状态并已提醒用户执行 commit；若尚未 commit，必须明确为“待提交完成”。
- 禁止“口头完成”，必须给出验证结果与未执行项。

# Approval Gates（命中即暂停并请求确认）

- 破坏性数据库变更（删表/删列/改类型/回填）
- 新增关键外部依赖（第三方 API/核心包）
- 删除或下线已上线能力
- 权限、密钥、安全策略变更
- 跨限界上下文大规模架构调整

# 关联规则（按需加载）

- 证据链与影响边界：`40-evidence-chain.mdc`
- 变更边界：`41-change-boundary.mdc`
- 验证输出：`42-verification-output.mdc`
- 纠错学习：`43-correction-learning.mdc`
- 注释同步：`44-comment-sync.mdc`
- 需求完整性：`45-requirement-intake.mdc`
