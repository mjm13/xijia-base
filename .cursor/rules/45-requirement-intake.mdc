---
description: 需求完整性闸门（Gate-0）：信息不足先补齐，不可落地先拒绝
---

# 目标

在进入分级与实现前，先判定需求是否完整可落地，避免“仅模块名开工”或“未确认原型差异直接实现”。

# Gate-0（需求完整性闸门）

进入分级前必须执行 Gate-0，输出 `verdict: complete | partial | reject`。

## 完整性维度（逐项检查）

1. 数据流闭环明确（来源 → 加工 → 去向）：
   - 每个 In-Scope 能力必须给出完整数据流三段，缺一段即视为未闭环：
     - 来源(Source)：`真实API | 外部系统对接 | 已有表/字段 | 仅种子(demo) | 人工录入` + 具体落点
     - 加工(Process)：校验 / 转换 / 业务规则 / 状态迁移 / 编排（无加工须显式写「直通」）
     - 去向(Sink)：目标表/字段 | 状态 | 返回结构 | 前端展示 | 外部系统
   - 任一段为空、占位符或标 `[待确认]` → 该能力未闭环，不得纳入 `complete`。
   - 若来源为 `仅种子(demo)`，须在 `Open Questions & Deferred` 写明真实来源待补。
   - `green-trivial` 场景（配置/样式/文档等无业务数据流）允许不填逐能力闭环表，但必须在需求文档显式写明 `本需求无数据流（green-trivial）`。
2. 原型对齐明确：
   - 若存在对应原型页面（位置见 `AGENTS.md` 的「UI 参考」；原型可能位于外部参考仓库），必须产出 Deviation Ticket（原型差异审批单）。
   - Deviation Ticket 未 `approved` 前，不得将该差异能力放入 In-Scope。
   - 若本需求确无可对照原型，在 Gate-0 输出中显式声明「无原型对照」，该维度按满分但须备注。
3. 验收可度量：
   - 每条 In-Scope AC 必须可手动或自动验证。
4. 未描述细节显式化：
   - 对需求未给出、但实现必须决定的点（UI 形态、字段、状态机、交互）列“未描述细节清单”。
   - 每项标记 `[假设]`，并在 Gate-1 前得到人工确认。

## 判定规则

- `complete`：上述维度均完整，可进入分级/判型。
- `partial`：部分可落地；仅将信息充分部分纳入 In-Scope，其余写入 Deferred，并向用户报告。
- `reject`：关键信息不足无法安全推进；必须 `stop-and-report`，列缺失清单并请求补充。
- 对 `partial/reject` 产生的 Deferred，收尾前必须写入 `docs/requirements/backlog.md`。

## 数据流闭环门禁（硬门禁，不闭环不得进入下一阶段）

进入分级 / 判型前，必须产出「数据流闭环表」（每个 In-Scope 能力一行：来源 → 加工 → 去向 → 闭环状态）。

- 闭环定义：每条能力的来源可经加工流向明确去向，三段均 `已确认`，无空段、无占位符、无 `[待确认]`。
- 任一能力未闭环时：
  - `verdict` 不得为 `complete`；
  - 仅将已闭环能力纳入 In-Scope（其余 Deferred），或整体 `reject`；
  - 必须 `stop-and-report`，列出断点（哪条能力的哪一段缺失）。
- 机器校验（断点即非零退出）：
  - `python .cursor/hooks/pipeline_guard.py --check-intake --req docs/requirements/inbox/<file>.md`
  - `green-trivial` 需 frontmatter 含 `tier: green-trivial`（或 `--tier green-trivial` 与 frontmatter 一致）

## 分级与 frontmatter（Gate-0 通过后、判型时必填）

进入「分级与判型」时，Agent 必须同步更新 requirement 文档 **YAML frontmatter**：

- `tier`：`green | green-trivial | yellow | red`（路由与 guard 真相源）
- `type`：`business | technical | hybrid`
- `openspec_change`：`tier=red` 时必填（kebab-case）

正文「分级与判型」表格中的 Tier / Change Type 必须与 frontmatter 一致；Tier Rationale 与 Tier Decision Matrix 仍写在正文。

## 二次确认回环（不得一次放行）

Gate-0 不得以「Agent 判定 + 用户一次点头」即通过，必须经过至少两轮确认：

1. 第一轮（缺口确认）：Agent 产出闭环表 + 断点清单，`stop-and-report` 请用户补齐断点。
2. 复核：用户补充后，Agent **重建闭环表并逐段复核**，将上一轮 `[待确认]` 改判为 `已确认` 或仍存疑；不得因「用户已回话」直接放行。
3. 第二轮（闭环终认）：闭环表全部 `已确认` 后，向用户出示最终闭环表并请其确认（签字）。
4. 仅当用户对最终闭环表确认、且 `--check-intake` 通过，方可进入分级 / 判型。

## Intake Score（0-100）

按 4 个维度打分，每维 0-25 分，总分 100：

1. 数据流闭环清晰度（来源→加工→去向）（0-25）
2. 原型对齐确认度（0-25）
3. 验收可测性（0-25）
4. 范围边界清晰度（0-25）

阈值映射：

- `>= 80`：`complete`
- `50-79`：`partial`
- `< 50`：`reject`

# 与证据链规则的关系

- 数据来源不清晰等价于 `40-evidence-chain.mdc` 的“数据落点证据断点”。
- 出现证据断点时，只能输出 `[待确认]`，不得写成确定结论。

# 输出模板

```text
Gate-0 完整性判定：
- verdict: complete|partial|reject
- intake_score: <0-100>
- 数据流闭环表（来源→加工→去向）：
  | 能力/AC | 来源(Source) | 加工(Process) | 去向(Sink) | 闭环 |
  |---|---|---|---|---|
  | <能力A> | ... | ... | ... | 已确认|待确认 |
- 闭环断点（未闭环项）：
  - <能力X>.<来源|加工|去向>: 缺失说明
- 二次确认状态：第一轮缺口确认 | 复核完成 | 用户闭环终认
- 原型对齐差异：
  - <差异项1> [待人工确认|已确认]
  - <差异项2> [待人工确认|已确认]
- 未描述细节清单：
  - <细节项1> [假设]
  - <细节项2> [假设]
- 可落地范围（In-Scope）：
  - ...
- Deferred / 拒绝原因：
  - ...
```

# Deviation Ticket（原型差异审批单）模板

```text
Deviation Ticket:
- id: DEV-<yyyyMMdd>-<nn>
- 原型入口：<文件/菜单/页面>
- 差异点：...
- 风险评估：...
- 替代方案：...
- 建议方案：对齐原型|偏离原型（说明原因）
- 审批状态：open|approved|rejected
- 确认人/时间：...
```
