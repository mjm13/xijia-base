---
description: 变更前先声明模块边界与影响面，执行最小必要修改，禁止顺手重构
alwaysApply: true
---

# 目标

确保每次改动都可解释、可回归、可追溯：只改必须改的闭环，不扩大无关变更。

# 基本原则

- 先确认所属模块与能力边界，再改代码。
- 优先最小必要变更，不做无关格式化、重命名、结构调整。
- 公共逻辑或跨模块变更视为高风险，必须先说明影响面。

# 实施要求

1. 改动前输出边界说明（见下方模板）。
2. 若必须改公共组件、共享流程、跨模块契约：
   - 说明为什么不能在模块内闭环完成。
   - 列出已知调用方与回归点。
3. Bug 修复只修改导致问题的最小闭环，不因为“复用方便”顺带调整其他模块。
4. 新需求优先落入现有模块；只有现有边界无法承载时，才提出新模块方案并说明理由。

# 禁止事项

- 未说明边界直接改公共逻辑
- 顺手重构无关代码
- 未评估调用方就修改共享契约
- 将“实现更优雅”作为扩大改动范围的唯一理由

# 输出模板

```text
模块边界说明：
- 所属模块/能力：...
- 计划改动文件：...
- 是否涉及公共逻辑或跨模块：是/否
- 影响面与调用方：...
- 回归验证点：...
```
