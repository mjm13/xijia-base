---
description: 变更边界门禁（只改最小必要闭环）
alwaysApply: false
globs:
  - backend/**
  - frontend/**
  - docs/openspec/**
---

# 目标

确保每次改动可解释、可回归、可追溯，避免无关扩散改动。

# 约束

1. 改动前明确模块边界、计划文件、影响调用方与回归点。
2. 涉及公共逻辑或跨模块契约时，必须说明不能在模块内闭环的原因。
3. Bug 修复仅修改导致问题的最小闭环，不顺手重构无关代码。

# 禁止事项

- 未说明边界直接改公共逻辑
- 未评估调用方就改共享契约
- 以“更优雅”为唯一理由扩大范围

# 输出模板

```text
模块边界说明：
- 所属模块/能力：...
- 计划改动文件：...
- 是否涉及公共逻辑或跨模块：是/否
- 影响面与调用方：...
- 回归验证点：...
```
