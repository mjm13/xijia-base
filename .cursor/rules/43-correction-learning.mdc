---
description: 用户纠正学习（验码后沉淀到知识文档与记忆）
---

# 触发条件

当用户明确纠正 AI 口径，且纠正涉及术语、边界、流程、规则或数据语义时触发。

# 执行要求

1. 先用代码或文档证据验证纠正内容。
2. 验证成立后：
   - 按 `xijia-memory` ensure scaffold 后，追加写入 `docs/memory/decisions.jsonl`（episodic 记忆；init 后可用）
   - 并将稳定语义追加到 `docs/domain/*` 或 ADR/计划文档（不覆盖历史）
3. 每次沉淀需附最小证据摘要：入口/契约/执行点/数据落点。
4. 无法闭环验证时标记 `[待确认]`，不得作为已确认事实写入。

# 输出模板

```text
纠错学习记录：
- 用户纠正：...
- 验证证据：入口... / 契约... / 执行点... / 数据落点...
- 记忆写回：docs/memory/decisions.jsonl
- 知识去向：docs/domain/... 或 docs/decisions/...
- 状态：confirmed | 待确认
```
