---
description: 用户明确纠正 AI 口径时，验证后将有效知识归类沉淀到现有知识文档
alwaysApply: false
---

# 目标

把“用户纠正”转化为可复用知识，减少同类误判重复出现。

# 触发条件（同时满足）

1. 用户明确否定 AI 说法并给出正确口径（如“不对/错了/应该是/不是 X 是 Y”）。
2. 纠正内容涉及至少一类：业务术语、入口路径、接口契约、数据语义、规则落点。

# 不触发场景

- 纯需求新增或范围调整（非纠错）
- 用户仅要求“改代码”，未纠正认知口径
- 未形成可验证证据的模糊表达

# 执行步骤

1. 提取纠正对：`原说法 -> 正说法`。
2. 验证证据：代码、文档、契约、测试结果至少命中一类。
3. 归类写入（只追加，不覆盖历史）：
   - 术语语义 -> `docs/domain/ubiquitous-language.md`
   - 数据语义/状态约束 -> `docs/domain/data-dictionary.md`
   - 模块与入口能力映射 -> `docs/capability-map.md`
   - 业务规则与不变量 -> `docs/domain/domain-model.md` 或 ADR
4. 证据未闭合时标记“待验证”，不得写成高置信结论。

# 输出模板

```text
纠正同步摘要：
- 触发：...
- 原说法 -> 正说法：...
- 证据：...
- 归类：术语/数据语义/能力映射/业务规则
- 已更新：...
- 未写入：...
```

# 禁止事项

- 未验证即沉淀为确定事实
- 覆盖或删除历史纠正记录
- 将“实现细节术语”直接写入领域统一语言
