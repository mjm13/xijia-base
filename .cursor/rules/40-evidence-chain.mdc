---
description: 需求分析、实现计划与 Bug 定位前必须建立证据闭环，禁止无证据推断
alwaysApply: true
---

# 目标

在输出方案、判断影响范围、定位根因之前，先建立可追溯证据链，避免“按名称猜结论”。

# 适用场景

- 新需求分析、方案设计、WBS 拆解
- 变更影响范围评估
- Bug 定位与根因判断

# 硬性要求

1. 不得仅凭需求描述、模块名、目录名、历史经验判断改动范围。
2. 方案输出前必须形成最小证据闭环：
   - 入口（页面/菜单/命令/API 调用方）
   - 契约（接口路径、关键参数、返回语义）
   - 执行点（后端函数/服务/任务）
   - 数据落点（表/状态字段/持久化资源）
3. 证据未闭合时只能标记 `[待确认]`，不得写成确定结论。
4. 用户指出遗漏入口或角色时，必须回到取证步骤重建证据链。

# Bug 场景补充

- Bug 场景必须补充“复现路径 -> 实际结果 -> 期望结果 -> 证据链断点”。
- 修复前必须给出当前假设及未确认项。
- 调试方法遵循 `systematic-debugging` skill，不得跳过取证直接改码。

# 输出模板

```text
影响范围证据摘要：
- 入口：...
- 契约：...
- 执行点：...
- 数据落点：...
- 已确认范围：...
- 待确认：...
```

```text
Bug 根因证据摘要：
- 复现路径：...
- 实际结果：...
- 期望结果：...
- 入口/契约/执行点/数据落点：...
- 当前假设：...
- 待确认：...
```
