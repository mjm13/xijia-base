---
description: 核心业务逻辑变更时同步更新语义注释，注释格式遵循 xijia-comment-enhancer
alwaysApply: false
---

# 目标

确保代码语义注释与业务行为同步，避免“实现已变、注释仍旧”。

# 触发场景

- 修改对外入口方法（接口/命令/事件处理入口）的业务逻辑
- 修改内部核心业务方法（规则判断、流程编排、关键转换）
- 新增核心业务方法

仅改注释文本且无逻辑变化时，不触发本规则。

# 执行要求

1. 逻辑变更后，同步更新对应语义注释。
2. 端点层与内部层维度遵循 `xijia-comment-enhancer`，不得混用标签。
3. 注释内容必须可由代码或契约取证，不得臆测业务语义。
4. 完成说明中必须给出“已更新方法/无需更新原因”。

# 输出模板

```text
注释同步：
- 已更新：<文件/符号列表>
- 无需更新：<原因>
```
