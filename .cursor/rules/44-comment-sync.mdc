---
description: 核心业务逻辑变更时同步语义注释（写代码阶段强制，verify 兜底）
---

# 触发条件（命中任一即必须执行）

- 本次 change **新增或修改**核心业务代码：API 端点、Service/业务处理单元、跨模块编排、复杂私有方法
- 本次 change 新增或修改核心业务模型 / 持久化模型（如承载菜单、角色、权限、组织等业务语义的 ORM/Entity）
- 修改核心业务规则、关键流程分支
- 用户要求补全/增强注释

# 可跳过白名单（仅以下情形允许跳过，须逐条对应到 diff）

- 本次 **仅** 改文档、配置、样式、依赖版本，未触达核心业务代码
- 本次 **仅** 改测试文件，且未改被测业务实现
- guard 报告的非业务壳层文件（入口 `main.ts`、`router`、`style.css`、构建配置等）——须在输出中点名并说明其非业务性质

> 「赶进度 / 先收尾 / 稍后补 / deferred」**不是**合法跳过理由。触达业务代码却未补注释，必须先写入 `docs/requirements/backlog.md` 并标注为阻塞项，不得宣告完成。

# 写代码阶段门禁（不得推迟到 verify）

1. **新增核心业务代码时**：同一次编辑必须按 `xijia-comment-enhancer` 格式输出语义注释；不得先写裸代码、收尾再补。
2. **修改核心业务代码时**：必须同步检查并更新该方法/类/端点的既有语义注释；注释缺失时同改动一起补齐。
3. Agent 在开始编辑核心业务文件前，必须先声明将同步注释的目标层级（内部层 / 端点层 / 模型层）与文件清单。
4. 若 afterEdit guard 提醒文件无语义注释标签，下一步必须优先补该文件注释，除非该文件属于白名单并向用户说明。

# 执行要求

1. **必须**调用 `xijia-comment-enhancer` 技能，按该技能规范执行（证据驱动、自下而上、只改注释不改逻辑）。
2. 顺序：先内部层（Service/Helper）→ 再端点层（API/Router）。
3. 注释与代码语义保持一致，避免模板化空话；端点层与内部层维度不得混用。
4. 逻辑有变更时，端点/内部层注释须追加 `[修改记录]`。

# verify 兜底门禁（客观校验，不接受纯自陈）

- 收尾时**必须**运行：`python .cursor/hooks/pipeline_guard.py --check-comment-sync`，并在验证输出中附其结论。
- guard exit≠0（存在无语义注释的变更实现文件）→ **不得**进入 Gate-2 或宣告完成；要么补注释、要么按白名单逐条说明并经用户确认。
- guard 为文件级启发式（只校验「文件是否含语义标签」），仍需人工确认本次改动的函数已覆盖。
- 验证输出格式：
  - `Comment Sync: done — <文件清单> — guard: pass`
  - 或 `Comment Sync: skipped — <白名单条目> — guard: pass(无实现变更)`
