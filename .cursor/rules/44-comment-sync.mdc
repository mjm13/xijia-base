---
description: 核心业务逻辑变更时同步语义注释（verify 阶段强制）
---

# 触发条件（命中任一即必须执行）

- 本次 change **新增或修改**核心业务代码：API 端点、Service/业务处理单元、跨模块编排、复杂私有方法
- 修改核心业务规则、关键流程分支
- 用户要求补全/增强注释

# 可跳过（须在 verify 输出中说明原因）

- 本次 **仅** 改文档、配置、样式、依赖版本，未触达上述代码
- 本次 **仅** 改测试文件，且未改被测业务实现

# 执行要求

1. **必须**调用 `xijia-comment-enhancer` 技能，按该技能规范执行（证据驱动、自下而上、只改注释不改逻辑）。
2. 顺序：先内部层（Service/Helper）→ 再端点层（API/Router）。
3. 注释与代码语义保持一致，避免模板化空话；端点层与内部层维度不得混用。
4. 逻辑有变更时，端点/内部层注释须追加 `[修改记录]`。

# verify 门禁

- 未执行注释同步且属于「触发条件」范围 → **不得**进入 Gate-2 或宣告完成。
- 跳过须在验证输出中写明：`Comment Sync: skipped — <原因>`。
- 已执行须在验证输出中列出：`Comment Sync: done — <文件清单>`。
