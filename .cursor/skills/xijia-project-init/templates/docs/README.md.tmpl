# 文档索引（Docs Index）

本目录承载 **{{project_name}}** 的需求、方案、规格、决策与领域知识沉淀。

## 核心目录职责

- `requirements/`：记录“要做什么 + 如何验收 + 🟢/🟡 实现方案”（需求入口、分级判型、实现方案、验收记录、状态流转，单文件单一真相源）
- `requirements/backlog.md`：沉淀 Gate-0 `partial/reject` 与收尾 Deferred 项的统一任务池
- `openspec/`：承载 🔴 核心需求的规格驱动变更（proposal/design/tasks/spec）
- `decisions/`：ADR，记录“为什么这样决策”
- `domain/`：已发布领域语义（AI 读现状只读这里；🔴 开发中草稿在对应 change 文件夹）

## 何时写到哪里

1. 新需求先进入 `requirements/inbox/`
2. 若为 🟢/🟡/🟢-trivial，分级判型、实现方案与验收记录直接写进该 requirement 文档（不再单建 `plans/`）
3. 若为 🔴，变更产物落 `openspec/changes/`
4. 涉及关键权衡时，补充 `decisions/`（ADR）
5. 所有档位收尾都执行 `sync-knowledge`；其中 🔴 可回灌 `domain/`，🟢/🟡/🟢-trivial 按需更新 `decisions/`、`capability-map.md`
6. 若本次存在 Deferred，先写入 `requirements/backlog.md` 再收尾

## 分档路由

- 🟢 轻量：`requirements`（方案与验收写在需求文档内）
- 🟢-trivial：`requirements`（显式声明“本需求无数据流（green-trivial）”）
- 🟡 中等：`requirements`（必要时补 ADR / domain）
- 🔴 核心：`requirements` + `openspec` + `decisions` + `domain`

## 按需创建文件

- `architecture.md`：需求收尾阶段需要沉淀架构决策时创建/更新
- `capability-map.md`：需求收尾阶段命中能力追溯触发条件时创建/更新
