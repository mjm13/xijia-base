---
title: <requirement-title>
status: inbox
owner: <owner>
createdAt: <yyyy-mm-dd>
---

# 背景

描述业务背景、触发原因和目标。

# 业务目标

- 目标 1
- 目标 2

# 用例 / 用户故事

1. 作为 <角色>，我希望 <行为>，以便 <收益>。

# 涉及限界上下文

- `<bounded-context-1>`

# 关键领域概念（统一语言）

- `<term-a>`：定义

# 领域规则与不变量

- 规则 1

# 范围切分（需求无法一次做完时）

## 本切片 In Scope

- 本次要打通的链路与能力

## Out of Scope

- 明确不在本次范围内

## Open Questions & Deferred

- 尚未想清、需后续切片或 spike 解决的问题

# 数据来源声明（Gate-0 必填）

- `<功能A>`：真实API | 外部系统对接 | 仅种子(demo) | 人工录入
- `<功能B>`：真实API | 外部系统对接 | 仅种子(demo) | 人工录入

# 原型偏离审批单（Deviation Ticket，有原型时必填）

- id：`DEV-<yyyyMMdd>-<nn>`
- 原型入口：`<原型文件/菜单/页面>`
- 差异点：...
- 风险评估：...
- 替代方案：...
- 建议方案：对齐原型 | 偏离原型（说明原因）
- 审批状态：open | approved | rejected
- 确认人/时间：...

# 完整性判定（Gate-0）

- verdict：complete | partial | reject
- intake_score：0-100（数据来源/原型对齐/验收可测/范围边界）
- 可落地范围（In Scope）：...
- Deferred / 拒绝原因：...
- 未描述细节清单（需人工确认）：...

# Spike 探针记录（链路不清时必填）

- 探针问题：...
- 假设列表：
  - H1：...
  - H2：...
- 最小实验与证据：
  - E1：...（证据：...）
  - E2：...（证据：...）
- 结论：成立 | 不成立 | 部分成立
- 推荐去向：green | yellow | red | reject
- 对 In-Scope 的影响：...

# 验收标准

- [ ] GIVEN ... WHEN ... THEN ...

# 验收记录（每个切片收尾追加）

## <切片 / change 名> — <yyyy-mm-dd>

- 涉及菜单 / 模块：<moduleKey + 路径，或「后端 API / 无界面」>
- 改了什么功能：<一句话>
- 验收场景：GIVEN ... WHEN ... THEN ...
- 手动验证步骤：<步骤与预期>
- 自动化覆盖：<测试名 / 命令>
- Gate-2 验收检查单：
  - AC-1：步骤...；预期...；结果（pass/fail）...
  - AC-2：步骤...；预期...；结果（pass/fail）...
- 本次范围外 / Deferred：<对齐上文 Open Questions>
