# Episodic Memory

本目录存放**过程层**研发记忆，供跨会话复用决策与纠错学习；**不是**长期领域真相源。

## 文件

- `decisions.jsonl`：结构化决策记录（JSONL，一行一条）
- `decisions.archive.jsonl`：过期条目归档（由 `memory_prune.py` 首次运行时按需生成，只追加，不手动编辑）

## 与长期知识的关系

| 层级 | 路径 | 用途 |
| --- | --- | --- |
| 过程记忆 | `docs/memory/decisions.jsonl` | checkpoint、用户纠正、临时决策 |
| 长期真相 | `docs/domain/*`、`docs/decisions/*.md` | 已确认、可审计的领域与架构决策 |

稳定语义应经 `sync-knowledge` 或人工确认后蒸馏到 ADR/domain，memory 条目可标记过期（`staleness`）。

## 写入入口

- `xijia-memory` 技能（任务完成、收尾、用户纠正后）
- `sync-knowledge` 归档回灌
- `43-correction-learning` 纠错学习

## 生命周期

- 读取：任务起点 just-in-time 读取相关 BC/tags 未过期条目（`50-context-engineering` 规则 5）
- 蒸馏：高 confidence 稳定条目经人确认提升到 ADR/`docs/domain`（`sync-knowledge` 7a）
- 剪枝：过期条目自动归档到 `decisions.archive.jsonl`，信息不删除（`sync-knowledge` 7b / `memory_prune.py`）
