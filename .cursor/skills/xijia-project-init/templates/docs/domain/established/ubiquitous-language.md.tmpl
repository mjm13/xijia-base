# Ubiquitous Language（Established）

| 术语 | 定义 | BC | 锚点（capability/moduleKey） | Aliases to AVOID |
| --- | --- | --- | --- | --- |
| <term> | <definition> | <bounded-context> | <capability-or-moduleKey> | <alias1, alias2> |

## 约束

- 新增术语必须落在明确 BC 内，不允许跨 BC 复用同名异义词。
- 每次新增术语时，至少补 1 个 `Aliases to AVOID`，防止同义词漂移。
- 术语命名优先与 OpenSpec capability 对齐。
