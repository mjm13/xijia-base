# Context Map（Established）

> 本文件除关系图外，同时作为 Bounded Context 增量注册表：每次迭代只登记本次涉及 BC，无需回填历史全量。

## Bounded Contexts

- <context-a>
- <context-b>

## Relationship Matrix

| Upstream | Downstream | Pattern | Contract |
| --- | --- | --- | --- |
| <context-a> | <context-b> | OHS/ACL/Conformist/Partnership | <semantic-contract> |

## Notes

- 上下文关系必须明确集成模式（Pattern），禁止仅写“调用关系”。
- 出现同词异义或 BC 边界冲突时先停下并人工确认，再写入关系矩阵。

## 修订记录（动态更新）

| 日期 | change-id | 操作（ADD/UPDATE/RENAME/DEPRECATE） | BC/关系 | 说明 |
| --- | --- | --- | --- | --- |
| <yyyy-mm-dd> | <change-name> | ADD | <context-a -> context-b> | <pattern/contract reason> |
