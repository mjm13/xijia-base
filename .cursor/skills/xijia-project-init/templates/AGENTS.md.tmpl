# AGENTS.md — {{project_name}} 协作入口地图

> 本文件是入口路由，不是规则正文。规则真相源位于 `.cursor/rules/`。
> 保持精简（建议 <150 行）；随架构与命令变化增量更新；禁止引用已删除脚本/命令。

## 0. 第一步读什么

| 想做的事 | 先读 | 说明 |
| --- | --- | --- |
| 初始化空仓库 | `/xijia:init` | 生成文档骨架与初始结构 |
| 推进具体需求 | `.cursor/rules/00-workflow.mdc` | 分级/判型/门禁/收尾 |
| 了解文档职责 | `docs/README.md` | 目录职责、分档路由、沉淀位置 |

## 1. 统一研发流水线

`explore -> propose -> apply -> verify -> sync -> archive -> sync-knowledge -> commit`

- 手动入口：`/xijia:start`
- 自动入口：`feature-pipeline`
- 状态命令：`/xijia:status`、`/xijia:stop`

## 2. 项目信息

- 项目：{{project_name}}
- 目标：{{project_goal}}
- 初始模块：{{primary_modules}}
- 技术栈（确认后补）：<backend/frontend/db/deploy + 版本>
- 维护者：{{author}}

## 3. 代码结构与运行方式

> 活文档：init 阶段不填实质内容。后端/前端骨架完成后，在对应技术 change **收尾**时增量补充。

- 代码结构：<待骨架完成后补充：各顶层目录与职责>
- 运行方式：<待补充：本地启动 / 测试 / 构建命令>
- 关键入口：<待补充，或链接 `docs/capability-map.md`>

## 4. 本地命令（可复制）

> 仅填写真实可执行命令（含必要参数）；不要写“使用 xxx 工具”这类模糊描述。

- 安装依赖：<待补充>
- 开发启动：<待补充>
- 构建：<待补充>
- 全量测试：<待补充>
- 单测定位：<待补充>

## 5. 测试说明

- 测试栈：<待补充，如 pytest/vitest>
- 约定：<待补充，如哪些场景必须补测试>
- Mock 边界：<待补充，如禁止 mock DB / 允许 mock 外部接口>

## 6. 安全与边界

- 密钥与凭据：<待补充，禁止提交规则>
- 高风险操作：<待补充，需人工确认的操作类型>
- 禁改文件/目录：<待补充>

## 7. 提交与 PR 约定

> 规则正文不写在此处，统一以项目规则为准。

- 提交规范：遵循 `xijia-git-commit` 技能
- 收尾门禁：遵循 `.cursor/rules/00-workflow.mdc`
- 合并前检查：<待补充，lint/test/build 等>
